# Fdw.Services.SecretManagers.Sqlite

The SQLite secret store.

This package declares 1 configuration type(s).

## Options (1)

| Type | Kind | Purpose |
|---|---|---|
| `SqliteSecretManagerConfigurationCommand` | class | — |

## Configuration (1)

| Type | Kind | Purpose |
|---|---|---|
| `SqliteSecretManagerConfiguration` | class | Configuration for the SQLite secret management service. Stores and retrieves secrets from a SQLite… |

## Types (1)

| Type | Kind | Purpose |
|---|---|---|
| `SqliteSecretManagerConfigurationValidator` | class | Validator for . |

## Installation

```bash
dotnet add package Fdw.Services.SecretManagers.Sqlite --prerelease
```

## Dependencies

`Fdw.Configuration.Abstractions` · `Fdw.Services` · `Fdw.Services.SecretManagers`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.Configuration.SourceGenerators` · `Fdw.Data.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
