# Fdw.UI.Components.RazorConsole

Razor components rendered to a console surface.

## Types (3)

| Type | Kind | Purpose |
|---|---|---|
| `CascadingParameterAttribute` | class | Marks a property as a cascading parameter. Cascading parameters flow down the component tree. |
| `ParameterAttribute` | class | Marks a property as a component parameter. Parameters can be set by parent components. |
| `RazorConsoleComponent<TModel>` | class | Base class for RazorConsole components. Inherits from TUIComponent to provide Spectre.Console… |

## Installation

```bash
dotnet add package Fdw.UI.Components.RazorConsole --prerelease
```

## Dependencies

`Fdw.UI.Abstractions` · `Fdw.UI.Components.TUI`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.UI.Components.RazorConsole.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
