# Fdw.Web.Http.Abstractions

HTTP contracts shared by clients and hosts.

Implementations live in the sibling packages; this one holds the interfaces, base types and models they agree on, so a consumer can depend on the contract without taking the implementation.

## Interfaces (3)

| Type | Kind | Purpose |
|---|---|---|
| `IEndpointType` | interface | Interface for endpoint type enhanced enums. Provides abstraction for endpoint classification and… |
| `IRateLimitPolicy` | interface | Interface for rate limit policy type options. Provides abstraction for rate limiting strategies. |
| `ISecurityMethod` | interface | Interface for security method type options. Provides abstraction for security authentication methods. |

## Base types (3)

| Type | Kind | Purpose |
|---|---|---|
| `EndpointTypeBase` | class | Type option defining different types of endpoints supported by the Fdw Web Framework. Each type provides… |
| `RateLimitPolicyBase` | class | Enhanced enum defining rate limiting policies for endpoint protection. Provides various strategies for… |
| `SecurityMethodBase` | class | Enhanced enum defining security authentication methods supported by the framework. Provides extensible… |

## Models and supporting types (19)

| Type | Kind | Purpose |
|---|---|---|
| `ApiKey` | class | API key-based authentication. |
| `Certificate` | class | Client certificate authentication. |
| `CommandEndpoint` | class | State-changing business operations endpoint type. |
| `Concurrency` | class | Concurrent request limiting. |
| `CrudEndpoint` | class | Get, Read, Update, Delete operations endpoint type. |
| `EndpointTypes` | class | Collection of endpoint types for HTTP operations. Generated by TypeCollectionGenerator with… |
| `EventEndpoint` | class | Event publishing and subscription operations endpoint type. |
| `FileEndpoint` | class | File upload, download, and manipulation operations endpoint type. |
| `FixedWindow` | class | Fixed time window rate limiting. |
| `HealthEndpoint` | class | System health and monitoring endpoint type. |
| `JWT` | class | JSON Web Token authentication. |
| `None` | class | No authentication required. |

## Installation

```bash
dotnet add package Fdw.Web.Http.Abstractions --prerelease
```

## Dependencies

`Fdw.Collections` · `Fdw.Messages` · `Fdw.Results` · `Fdw.Services.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
