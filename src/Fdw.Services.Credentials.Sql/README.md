# Fdw.Services.Credentials.Sql

SQL-backed credential and data-vault services.

Registration lives in the option, not in a host's `Program.cs`. Each option carries three replaceable phase bodies — **Configure**, **Register**, **Initialize** — where the first two take and return the `IHostApplicationBuilder` and the third takes and returns the `IServiceProvider`. Referencing this package is what enlists its options: a module initializer collects and dedupes them at assembly load.

## Collections

| Type | Kind | Purpose |
|---|---|---|
| `CredentialServiceTypes` | class | Collection of credential service types. Generated by ServiceTypeCollectionGenerator with… |
| `DataVaultServiceTypes` | class | Collection of data vault service types. Generated by ServiceTypeCollectionGenerator with… |

## Configuration

Configuration classes are `[ManagedConfiguration]`: they generate their own DDL, validation and UI form metadata, and are read back as rows rather than from JSON.

| Type | Kind | Purpose |
|---|---|---|
| `CredentialServiceConfiguration` | class | Header configuration class for all credential service types. Generates the parent table… |
| `DataVaultConfiguration` | class | Header configuration class for all data vault types. Generates the parent table sec.DataVault which… |
| `DefaultDataVaultConfiguration` | class | Typed body configuration for the default data vault implementation. Persisted to sec.DefaultDataVault as… |
| `SqlCredentialServiceConfiguration` | class | Typed body configuration for the SQL credential service implementation. Persisted to… |

## Installation

```bash
dotnet add package Fdw.Services.Credentials.Sql --prerelease
```

## Dependencies

`Fdw.Abstractions` · `Fdw.Collections` · `Fdw.Commands.Data` · `Fdw.Commands.Data.Extensions` · `Fdw.Configuration` · `Fdw.Configuration.Abstractions` · `Fdw.Data.Abstractions` · `Fdw.MessageLogging.Abstractions` · `Fdw.Messages` · `Fdw.Results` · `Fdw.Security.Hashing` · `Fdw.Services` · `Fdw.Services.Abstractions` · `Fdw.Services.Authentication.Abstractions` · `Fdw.Services.Connections.Abstractions` · `Fdw.Services.Connections.MsSql` · `Fdw.Services.Credentials.Abstractions` · `Fdw.Services.Data.Abstractions` · `Fdw.Services.DataVault.Abstractions` · `Fdw.Services.SecretManagers` · `Fdw.Services.SecretManagers.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.Configuration.SourceGenerators` · `Fdw.Data.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
