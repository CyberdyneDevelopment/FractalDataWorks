# Fdw.Services.Notifications.Abstractions

The notification contracts: providers, priorities and delivery status.

Implementations live in the sibling packages; this one holds the interfaces, base types and models they agree on, so a consumer can depend on the contract without taking the implementation.

## Interfaces (14)

| Type | Kind | Purpose |
|---|---|---|
| `IGenericNotification` | interface | Base interface for notification services in the Fdw framework. All notification implementations must… |
| `INotificationChannel` | interface | Interface for notification channels. Defines the contract for notification delivery channels (Email,… |
| `INotificationConditionType` | interface | Interface for notification condition types with evaluation behavior. |
| `INotificationDispatcher` | interface | Interface for the notification dispatcher. Routes notification requests to the appropriate… |
| `INotificationFactory` | interface | Marker interface for notification factories. |
| `INotificationFactory<TNotification, TConfiguration>` | interface | Generic interface for notification factories with typed configuration. |
| `INotificationPriority` | interface | Interface for notification priority levels. |
| `INotificationRequest` | interface | Interface for notification requests. Extends IGenericCommand so notifications can flow through the… |
| `INotificationResult` | interface | Interface for notification results. Defines the outcome of sending a notification. |
| `INotificationService` | interface | Interface for channel-specific notification services. Each notification channel (Email, Teams, Webhook)… |
| `INotificationStatus` | interface | Interface for notification statuses. |
| `INotificationType` | interface | Non-generic interface for notification service types. Provides a common base for all notification types… |
| `INotificationType<TService, TConfiguration, TFactory>` | interface | Generic interface for notification service type definitions. Defines the contract for notification… |
| `IUserNotificationPreferenceService` | interface | Reads and writes per-user notification preferences (the notification-type / delivery-channel enabled… |

## Base types (8)

| Type | Kind | Purpose |
|---|---|---|
| `NotificationChannelBase` | class | Base class for notification channels. Implements the INotificationChannel interface with common… |
| `NotificationChannels` | class | Collection of notification channels. Generated by TypeCollectionGenerator with high-performance lookups.… |
| `NotificationConditionTypeBase` | class | Base class for notification condition types. |
| `NotificationConditionTypes` | class | Collection of notification condition types with evaluation behavior. |
| `NotificationPriorities` | class | TypeCollection for notification priority levels. |
| `NotificationPriorityBase` | class | Base class for notification priority levels. |
| `NotificationStatusBase` | class | Base class for notification statuses. |
| `NotificationStatuses` | class | TypeCollection for notification statuses. |

## Models and supporting types (34)

| Type | Kind | Purpose |
|---|---|---|
| `ConsecutiveFailuresCondition` | class | Fires after N consecutive failures. |
| `ConsoleChannel` | class | Console/log notification channel for development and test environments. Emits notification content via… |
| `DurationExceededCondition` | class | Fires if execution exceeds expected duration. |
| `EmailChannel` | class | Email notification channel. Sends notifications via SMTP email. |
| `ExecutionStatusCondition` | class | Fires on specific execution status (Failed, Succeeded, etc.). |
| `NotificationContext` | class | Context for evaluating notification conditions. |
| `NotificationListSummaryDto` | class | Summary DTO for a notification list, used in list views. |
| `NotificationPreference` | class | Domain model for a single user notification preference entry (a notification-type / delivery-channel… |
| `NotificationRequest` | class | Concrete implementation of a notification request. |
| `NotificationRequestBuilder` | class | Builder for creating notification requests. |
| `NotificationResult` | class | Concrete implementation of a notification result. |
| `NotificationRuleSummaryDto` | class | Summary DTO for a notification rule, used in list views. |

## Installation

```bash
dotnet add package Fdw.Services.Notifications.Abstractions --prerelease
```

## Dependencies

`Fdw.Abstractions` · `Fdw.Collections` · `Fdw.Configuration.Abstractions` · `Fdw.Results` · `Fdw.Services.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
