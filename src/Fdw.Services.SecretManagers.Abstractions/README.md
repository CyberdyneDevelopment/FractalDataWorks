# Fdw.Services.SecretManagers.Abstractions

The `ISecretManager` contract and the command shapes its stores implement.

Implementations live in the sibling packages; this one holds the interfaces, base types and models they agree on, so a consumer can depend on the contract without taking the implementation.

## Interfaces (16)

| Type | Kind | Purpose |
|---|---|---|
| `ISecretContainer` | interface | Interface representing a secret container (vault, store, namespace) within a secret provider. Provides… |
| `ISecretContainerUsage` | interface | Interface representing usage statistics for a secret container. Provides operational metrics and… |
| `ISecretManager` | interface | Main interface for secret management operations in the Fdw framework. Provides a unified facade for… |
| `ISecretManagerCommand` | interface | Interface for secret management commands in the Fdw framework. Represents a managementCommand that can… |
| `ISecretManagerCommand<TResult>` | interface | Generic interface for secret commands with typed result expectations. Extends the base managementCommand… |
| `ISecretManagerCommandHandler` | interface | Non-generic base interface for secret manager command handlers. Used by the TypeCollection for handler… |
| `ISecretManagerConfiguration` | interface | Marker interface for typed secret manager body configurations (MsSqlSecretManagerConfiguration,… |
| `ISecretManagerExecutionContext` | interface | Provides execution context and dependencies for secret manager command handlers. |
| `ISecretManagerProvider` | interface | Provider interface for secret manager services. Replaces the generic usage with a proper domain-specific… |
| `ISecretManagerResultCode` | interface | Interface for SecretManager result codes. |
| `ISecretManagerServiceFactory` | interface | Marker interface for secret manager factories. |
| `ISecretManagerServiceFactory<TSecretService, TConfiguration>` | interface | Generic interface for secret manager factories with typed configuration. |
| `ISecretManagerType` | interface | Non-generic interface for secret management service types. Provides a common base for all secret… |
| `ISecretManagerType<TService, TFactory, TConfiguration>` | interface | Interface for secret management service types. Defines the contract for secret management service type… |
| `ISecretMetadata` | interface | Interface representing metadata information about a secret without exposing the secret value. Provides… |
| `ISecretType` | interface | Marker interface for secret types. |

## Base types (6)

| Type | Kind | Purpose |
|---|---|---|
| `SecretManagerCommands` | class | Collection of secret manager command types. Generated by TypeCollectionGenerator with high-performance… |
| `SecretManagerMessage` | class | Base class for all secret manager-related messages. |
| `SecretManagerMessageCollectionBase` | class | Collection definition to generate SecretManagerMessages static class. |
| `SecretManagerResultCodeBase` | class | Base class for SecretManager result codes. |
| `SecretManagerResultCodes` | class | TypeCollection for SecretManager result codes. Codes use categorized numbers (prefix "SECRETMANAGER");… |
| `SecretTypeBase` | class | Base class for secret type markers used to identify what type of secret credential a service requires. |

## Models and supporting types (27)

| Type | Kind | Purpose |
|---|---|---|
| `CertificateExportFailedCode` | class | Failed to export certificate. |
| `CertificateNameRequiredCode` | class | Certificate name is required for the operation. |
| `CommandNullMessage` | class | CurrentMessage indicating that the command was null. |
| `CreateSecretManagerPayload` | class | Request payload for creating a new secret manager configuration. |
| `CredentialStorageResult` | class | Result of a credential storage operation. |
| `CredentialVerificationResult` | class | Result of a credential verification operation. |
| `InvalidCommandTypeCode` | class | Command must be of the correct type for the handler. |
| `InvalidExecutionContextCode` | class | Execution context must be of the correct type. |
| `ListSecretManagersResponse` | class | Response DTO containing the names of all configured secret managers. |
| `ListSecretsFailedCode` | class | Failed to list secrets. |
| `NoHandlerFoundCode` | class | No handler found for the specified command type. |

## Installation

```bash
dotnet add package Fdw.Services.SecretManagers.Abstractions --prerelease
```

## Dependencies

`Fdw.Collections` · `Fdw.Configuration.Abstractions` · `Fdw.Services.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
