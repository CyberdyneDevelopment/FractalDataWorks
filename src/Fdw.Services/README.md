# Fdw.Services

The service infrastructure: the default providers, the factory registries options fill during Register, and the health-check surface shared across every domain.

Registration lives in the option, not in a host's `Program.cs`. Each option carries three replaceable phase bodies — **Configure**, **Register**, **Initialize** — where the first two take and return the `IHostApplicationBuilder` and the third takes and returns the `IServiceProvider`. Referencing this package is what enlists its options: a module initializer collects and dedupes them at assembly load.

## Collections

| Type | Kind | Purpose |
|---|---|---|
| `ConfigurationCommands` | class | TypeCollection of configuration command options. Each TypeOption is a concrete subclass of… |
| `HealthMonitorTypes` | class | ServiceTypeCollection for health monitor service types. Generated by ServiceTypeCollectionGenerator with… |
| `ServicesResultCodes` | class | TypeCollection for Services result codes. Codes use the categorized-number scheme: Id == EventId ==… |

## Options (1 declared)

| Type | Kind | Purpose |
|---|---|---|
| `LocalHealthMonitorType` | class | Service type for the in-process health monitor ("Local") — used by hosts that ARE the health source… |

Shipped options are reference implementations, not canon — a consumer adds a kind by declaring its own option against this collection, in its own assembly.

## Configuration

Configuration classes are `[ManagedConfiguration]`: they generate their own DDL, validation and UI form metadata, and are read back as rows rather than from JSON.

| Type | Kind | Purpose |
|---|---|---|
| `HealthMonitorConfiguration` | class | Configuration for a health monitor service instance — a settings.HealthMonitor row in ConfigurationDb. |

## Installation

```bash
dotnet add package Fdw.Services --prerelease
```

## Dependencies

`Fdw.Collections` · `Fdw.Commands.Data` · `Fdw.Commands.Data.Abstractions` · `Fdw.Commands.Data.Extensions` · `Fdw.Configuration` · `Fdw.Etl.Abstractions` · `Fdw.MessageLogging.Abstractions` · `Fdw.Messages` · `Fdw.Results` · `Fdw.Results.Abstractions` · `Fdw.Services.Abstractions` · `Fdw.Services.Configuration.Abstractions` · `Fdw.Services.Connections.Abstractions` · `Fdw.Services.Data.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.Configuration.SourceGenerators` · `Fdw.Data.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
