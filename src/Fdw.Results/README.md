# Fdw.Results

The result type FDW returns from anything that can fail: `GenericResult` / `GenericResult<T>`, the severity and category TypeCollections, and the result-code catalogue scheme.

A TypeCollection and its options — an extensible enum. Members are discovered at compile time and reachable by `ByName` / `ById` / `All`; a lookup that finds nothing returns a `NotFound` sentinel rather than null, so a caller never has to null-check a lookup.

A module initializer collects every `[TypeOption]` in every referenced assembly and dedupes them into the collection at load. Adding a member is a package reference plus a declaration — the collection itself is never edited.

## Collections

| Type | Kind | Purpose |
|---|---|---|
| `ExecutionStatuses` | class | Collection of execution status types for UI rendering. Generated by TypeCollectionGenerator with… |
| `ResultCategories` | class | Closed TypeCollection of result-code handling categories (1..11). The category is the leading band of a… |
| `ResultSeverities` | class | TypeCollection for result severity levels. LogLevel values: Trace=0, Debug=1, Information=2, Warning=3,… |
| `ResultStatuses` | class | TypeCollection for result status levels. |

## Options (28 declared)

| Type | Kind | Purpose |
|---|---|---|
| `AuthCategory` | class | Category 5 (50000–59999): authentication or authorization failure. |
| `CancelledStatus` | class | Cancelled execution status - execution was cancelled. |
| `ConfigurationCategory` | class | Category 6 (60000–69999): configuration / setup — missing, unconfigured, or unregistered setup. |
| `ConflictCategory` | class | Category 4 (40000–49999): conflict / state — already-exists, wrong state, or version conflict. |
| `CriticalSeverity` | class | Critical severity - system-level failure. |
| `DebugSeverity` | class | Debug severity - debugging information. |
| `DependencyCategory` | class | Category 7 (70000–79999): dependency / connection / IO — failure talking to an external system.… |
| `ErrorSeverity` | class | Error severity - operation failed. |
| `FailedStatus` | class | Failed execution status - execution failed with errors. |
| `FailureStatus` | class | Failure status - operation failed. |
| `ForbiddenCategory` | class | Category 10 (100000–109999): the caller is authenticated but not permitted to perform the action.… |
| `GatewayTimeoutCategory` | class | Category 11 (110000–119999): a downstream dependency did not respond in time. Distinct from (503,… |
| `InformationSeverity` | class | Information severity - general operational information. |
| `InternalCategory` | class | Category 9 (90000–99999): internal / unexpected — unexpected faults, not-implemented, and… |
| `MissingCategory` | class | Category 3 (30000–39999): not found / missing — a requested resource does not exist. |
| `PartialSuccessStatus` | class | Partial success - batch operation where some items succeeded and some failed. |
| `PendingStatus` | class | Pending execution status - execution is queued. |
| `RunningStatus` | class | Running execution status - execution in progress. |

## Installation

```bash
dotnet add package Fdw.Results --prerelease
```

## Dependencies

`Fdw.Collections` · `Fdw.Messages` · `Fdw.Results.Abstractions` · `Fdw.Types.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
