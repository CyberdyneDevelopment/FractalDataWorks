# Fdw.CodeBuilder.Analysis

Analysis contracts over built code.

This package declares 11 interface(s).

## Contracts (11)

| Type | Kind | Purpose |
|---|---|---|
| `IClassExpectations` | interface | Interface for class expectations. |
| `IConstructorExpectations` | interface | Interface for constructor expectations. |
| `IEnumExpectations` | interface | Interface for enum expectations. |
| `IFieldExpectations` | interface | Interface for field expectations. |
| `IInterfaceExpectations` | interface | Interface for interface expectations. |
| `IMethodExpectations` | interface | Interface for method expectations. |
| `INamespaceExpectations` | interface | Interface for namespace expectations. |
| `IPropertyExpectations` | interface | Interface for property expectations. |
| `IRecordExpectations` | interface | Interface for record expectations. |
| `ISyntaxExpectations` | interface | Interface for syntax tree expectations and verification. |
| `ISyntaxTreeExpectations` | interface | Interface for syntax tree expectations. |

## Installation

```bash
dotnet add package Fdw.CodeBuilder.Analysis --prerelease
```

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
