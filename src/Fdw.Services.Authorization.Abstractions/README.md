# Fdw.Services.Authorization.Abstractions

The authorization contracts — permissions, roles and the evaluation surface.

Implementations live in the sibling packages; this one holds the interfaces, base types and models they agree on, so a consumer can depend on the contract without taking the implementation.

## Interfaces (4)

| Type | Kind | Purpose |
|---|---|---|
| `IEffectivePermissionResolver` | interface | Resolves the effective permission set for a user by applying the 3-tier union: global-tenant ∪… |
| `IPathAuthorizationDecision` | interface | The recorded outcome of evaluating an against a resolved canonical address. Carried back through the… |
| `IPathAuthorizationPolicy` | interface | Evaluates whether an is authorized to access a resolved canonical address. Stateless; produces an . |
| `IPermissionAction` | interface | Interface for permission action types with UI rendering properties. |

## Base types (5)

| Type | Kind | Purpose |
|---|---|---|
| `AuthorizationMessage` | class | Base class for all authorization-related messages. |
| `PathAuthorizationPolicies` | class | TypeCollection of all s in the system. Downstream projects register policies via… |
| `PathAuthorizationPolicyBase` | class | Base class for path authorization policy TypeOptions. Concrete policies override to return an allow/deny… |
| `PermissionActionBase` | class | Base class for permission action type options. |
| `PermissionActions` | class | Collection of permission action types for authorization and UI rendering. Generated by… |

## Models and supporting types (29)

| Type | Kind | Purpose |
|---|---|---|
| `AdminAction` | class | Permission action for administrative operations. |
| `AdminOnlyPolicy` | class | Allows access only when the caller has the system-admin role. Use for sensitive paths like AllProjects… |
| `ApproveAction` | class | Permission action for approving items. |
| `AssignRolePayload` | class | Data transfer object for assigning a role to a user. |
| `AuthorizationDeniedMessage` | class | Message indicating authorization was denied. |
| `AuthorizationLogger` | class | High-performance logger for authorization events. |
| `BrowseAction` | class | Permission action for browsing/navigating resources. |
| `CreateAction` | class | Permission action for creating new resources. |
| `CreateRolePayload` | class | Data transfer object for creating a new role. |
| `DeleteAction` | class | Permission action for deleting resources. |
| `DenyAllPolicy` | class | Denies all access. Use for sealed paths or as a default-deny fallback when seed data references a policy… |
| `ExecuteAction` | class | Permission action for executing operations. |

## Installation

```bash
dotnet add package Fdw.Services.Authorization.Abstractions --prerelease
```

## Dependencies

`Fdw.Abstractions` · `Fdw.Collections` · `Fdw.MessageLogging.Abstractions` · `Fdw.Messages` · `Fdw.Results` · `Fdw.Services.Abstractions` · `Fdw.Services.Authentication.Abstractions` · `Fdw.Services.Multitenancy.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
