# Fdw.Services.Authorization

Authorization: permissions, roles and the checks a principal is evaluated against.

Registration lives in the option, not in a host's `Program.cs`. Each option carries three replaceable phase bodies — **Configure**, **Register**, **Initialize** — where the first two take and return the `IHostApplicationBuilder` and the third takes and returns the `IServiceProvider`. Referencing this package is what enlists its options: a module initializer collects and dedupes them at assembly load.

## Collections

| Type | Kind | Purpose |
|---|---|---|
| `AuthorizationTypes` | class | ServiceTypeCollection for authorization service types. Generated by ServiceTypeCollectionGenerator with… |

## Options (1 declared)

| Type | Kind | Purpose |
|---|---|---|
| `DefaultAuthorizationServiceType` | class | Default authorization service type that registers and ASP.NET Core authorization bridge components with… |

Shipped options are reference implementations, not canon — a consumer adds a kind by declaring its own option against this collection, in its own assembly.

## Configuration

Configuration classes are `[ManagedConfiguration]`: they generate their own DDL, validation and UI form metadata, and are read back as rows rather than from JSON.

| Type | Kind | Purpose |
|---|---|---|
| `PermissionConfiguration` | class | Database-backed configuration for permissions. Generates the table authz.Permission. |
| `RoleConfiguration` | class | Database-backed configuration for roles. Generates the table authz.Role. |
| `RolePermissionConfiguration` | class | Database-backed configuration for role-permission assignments. Generates the table authz.RolePermission… |
| `UserRoleConfiguration` | class | Database-backed configuration for user-role assignments. Generates the table authz.UserRole. |

## Installation

```bash
dotnet add package Fdw.Services.Authorization --prerelease
```

## Dependencies

`Fdw.Commands.Data` · `Fdw.Commands.Data.Extensions` · `Fdw.Configuration` · `Fdw.Data.Abstractions` · `Fdw.MessageLogging.Abstractions` · `Fdw.Results` · `Fdw.Services` · `Fdw.Services.Abstractions` · `Fdw.Services.Authentication.Abstractions` · `Fdw.Services.Authorization.Abstractions` · `Fdw.Services.Connections.Abstractions` · `Fdw.Services.Data.Abstractions` · `Fdw.Services.Multitenancy.Abstractions` · `Fdw.Validation` · `Fdw.Web.Http.Abstractions`

Build-time only (generators and analyzers, not runtime dependencies): `Fdw.Collections.SourceGenerators` · `Fdw.Configuration.SourceGenerators` · `Fdw.Data.SourceGenerators` · `Fdw.MessageLogging.SourceGenerators`

---

Part of **[FractalDataWorks](https://github.com/CyberdyneDevelopment/FractalDataWorks)** `1.0.0-rc.1`. Licensed under Apache-2.0.

<!-- generated from source at 4d75c3fab on 2026-08-04; regenerate rather than hand-edit -->
